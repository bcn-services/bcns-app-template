# @nseluga/* shared packages resolve from GitHub Packages (private registry).
# Supply GITHUB_TOKEN (PAT with read:packages) via env on any machine that installs —
# your laptop and the Coolify build box. Never commit the token itself.
@nseluga:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
